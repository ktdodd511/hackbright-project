Which loop should be used if you don't know the number times you need it iterated?
Where does one "talk" to the computer?
Where did the language Python get its name?
Python, Ruby, and Java are all what kind of languages?
What does the integer 0 return, when presented as a boolean?
What statement does a conditional NEED to have?
What is the output of print str[2:] if str = 'Hello World!'?
Which operator in Python performs exponential (power) calculation on operands?
The output of ['Hi!'] * 4 will return an error, True or False?
What is the function that returns the length of a list?
Which function converts a string to an integer in Python?
